export enum ButtonVariant {
  Primary = 'primary',
  Secondary = 'secondary',
  Media = 'media',
}

export enum WebcamImageSize {
  Full = '',
  Eighty = '80',
  Sixty = '60',
}

export interface IFeed {
  video: string
  screen: string
}
